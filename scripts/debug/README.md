# Debug Scripts 

**Purpose**: Retained diagnostic scripts historically used to confirm environment connectivity and package installation integrity.

**Contents**:
- `debug_imports.py` / `debug_models.py`: Sanity checks for Langchain vs local Python runtimes.
- `test_embedding.py` / `list_models.py`: API ping-checks testing if LLM quotas are behaving reliably.
